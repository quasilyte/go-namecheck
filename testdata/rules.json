{
    "^context\\.Context$|^\\w+Context$|^[Cc]ontext$": {
        "param+field": {"ctx": "ctxt"}
    },

    "regexp\\.Regexp": {
        "local+global": {
            "^re[A-Z]\\w*$": "use RE suffix instead of re prefix"
        }
    },

    "^func\\(.*?\\)": {
        "param": {"f": "fn"}
    }
}
